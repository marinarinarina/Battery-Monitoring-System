export interface LoadingSpinnerProps {
  size?: number;       // 스피너 크기(px), 기본 32
  color?: string;      // 스피너 색상, 기본 theme.colors.primary
  className?: string;
  'aria-label'?: string; // 접근성 레이블
}
