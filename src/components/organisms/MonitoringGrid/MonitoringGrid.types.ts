import { MonitoringCardProps } from '@/components/molecules/MonitoringCard/MonitoringCard.types';

export interface MonitoringGridProps {
  cards: MonitoringCardProps[]; // MonitoringCard에 넘길 props 배열
}